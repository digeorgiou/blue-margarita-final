services:
  - type: web
    name: bluemargarita-backend
    env: java
    buildCommand: cd backend && ./gradlew clean build -x test
    startCommand: cd backend && java -Dserver.port=$PORT -jar build/libs/*.jar
    envVars:
      - key: DATABASE_URL
        value: jdbc:postgresql://bluemargarita_user:7WrCF4uzAhDQVSNOIRnGxIoqCcYN6bCo@dpg-d2ucdb0gjchc73c4cve0-a.frankfurt-postgres.render.com:5432/bluemargaritadb
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://bluemargarita-frontend.onrender.com
      - key: SPRING_PROFILES_ACTIVE
        value: production

databases:
  - name: bluemargarita-db
    databaseName: bluemargaritadb
    user: bluemargarita_user